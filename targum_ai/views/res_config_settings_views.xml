<odoo>
    <record id="sync_all_products_server_action" model="ir.actions.server">
        <field name="name">Sync All Products to Targum</field>
        <field name="model_id" ref="product.model_product_template"/>
        <field name="state">code</field>
        <field name="code">
batch_size = 50
products = env["product.template"].search([])
total_products = len(products)

if total_products == 0:
    action = {
        'type': 'ir.actions.client',
        'tag': 'display_notification',
        'params': {
            'message': 'No products found to sync.',
            'type': 'warning',
        }
    }
else:
    synced_count = 0
    failed_count = 0
    
    for i in range(0, total_products, batch_size):
        batch = products[i:i + batch_size]
        
        for product in batch:
            try:
                product.with_context(skip_webhook=False)._send_webhook(product, "update")
                synced_count += 1
            except Exception as e:
                failed_count += 1
                print(f"Failed to sync product {product.name}: {str(e)}")
        
        env.cr.commit()
    
    message = f"Sync completed: {synced_count} products synced"
    if failed_count > 0:
        message += f", {failed_count} failed"
        
    action = {
        'type': 'ir.actions.client',
        'tag': 'display_notification',
        'params': {
            'message': message,
            'type': 'success' if failed_count == 0 else 'warning',
        }
    }
        </field>
    </record>

    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.targum</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="base.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="inside">
                <div class="app_settings_block" data-string="Targum AI" string="Targum AI">
                    <h2>Targum AI Integration</h2>
                    <div class="row mt16 o_settings_container">
                        <div class="col-12 col-lg-6 o_setting_box">
                            <div class="o_setting_right_pane">
                                <label for="integration_secret"/>
                                <field name="integration_secret" password="True"/>
                                <div class="text-muted">
                                    Secret key for authenticating webhooks to Targum
                                </div>
                            </div>
                        </div>
                        <div class="col-12 col-lg-6 o_setting_box">
                            <div class="o_setting_right_pane">
                                <label for="targum_instance_url"/>
                                <field name="targum_instance_url"/>
                                <div class="text-muted">
                                    Base URL of your Targum instance (e.g., https://api.targum.ai)
                                </div>
                            </div>
                        </div>
                        <div class="col-12 col-lg-6 o_setting_box">
                            <div class="o_setting_right_pane">
                                <label for="api_key"/>
                                <field name="api_key" widget="CopyClipboardChar"/>
                                <div class="text-muted">
                                    Auto-generated API key for Targum AI integration
                                </div>
                            </div>
                        </div>
                        <div class="col-12 o_setting_box">
                            <div class="o_setting_right_pane">
                                <button name="%(sync_all_products_server_action)d" string="Sync All Products to Targum" type="action" class="btn btn-primary" confirm="This will sync all products to Targum. For large catalogs, this may take several minutes. Please do not close this page during the sync. Continue?"/>
                                <div class="text-muted">
                                    Send all existing products to Targum
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </xpath>
        </field>
    </record>
</odoo>
